rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /project/{project} {
      allow read, write;
    }
    match /planning/{planning} {
      allow create: if request.auth.uid != null;
      allow list: if request.resource.owner == request.auth.uid;
      allow write: if request.resource.owner == request.auth.uid;
      allow read: if request.resource.owner == request.auth.uid;
    }
  }
}
